{% extends "layout.jinja2" %}
{% block content %}
    <div class="container" style="margin-top:10px;">
    <div class="card">
  <div class="card-body">
      <h3>Sign in</h3>
      {% if error %}
      <div class="alert alert-danger" role="alert">
          We're sorry, we could not find the username or password in our records, please try again:
      </div>
      {% endif %}
      {% if banned %}
      <div class="alert alert-danger" role="alert">
          This account has been banned.
      </div>
      {% endif %}

        <form method="post">
            <div class="form-group">
            <label for="uname">Email:</label>
            <input type="email" class="form-control" placeholder="bob@example.com" name="email" required>
            </div>
            <button type="submit" class="btn btn-primary pull-right">Send Me An Email</button>
            <p></p>
        </form>
  </div></div>
    </div>
{% endblock %}
